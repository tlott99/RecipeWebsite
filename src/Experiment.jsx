
// const Database = require("@replit/database")
export default function Experiment() {
  
  
  // const db = new Database()
  
  // db.set("recipe1", "{'name':'Spaghetti'}").then(() => {});
  
  return (
    <div>
      I am the experiment more stuff.
    </div>
  )
}